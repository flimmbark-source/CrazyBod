name: Apply cafe seating fix

on:
  push:
    branches:
      - feature/cafe-narrative-beat
    paths:
      - .github/workflows/apply-cafe-seating-fix.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/cafe-narrative-beat
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: python tools/apply_cafe_seating_fix.py
      - run: npm test
      - run: npm run build
      - name: Commit validated seating fix
        run: |
          rm tools/apply_cafe_seating_fix.py
          rm .github/workflows/apply-cafe-seating-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix cafe seating camera and chairs"
          git push origin HEAD:feature/cafe-narrative-beat
