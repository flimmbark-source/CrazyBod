name: Run café ordering flow

on:
  push:
    branches: [main]
    paths:
      - .github/cafe-ordering-flow-trigger

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/orchestrate-cafe-order-and-seating
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/apply-cafe-ordering-flow.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.jsx src/world/JourneyScene.jsx
          git diff --cached --quiet || git commit -m "orchestrate café ordering and seating"
          git push origin HEAD:fix/orchestrate-cafe-order-and-seating
